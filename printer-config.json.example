{
  "_comment": "Renomme en printer-config.json. hosts: tableau des serveurs d'impression Windows (IP ou FQDN). user/password/domain: compte AD avec droits sur tous les serveurs. snmpCommunity: utilisée pour la récupération du modèle d'imprimante (défaut 'public').",
  "hosts": ["PRINT_SERVER_1", "PRINT_SERVER_2"],
  "user": "REPLACE_ME",
  "password": "REPLACE_ME",
  "domain": "REPLACE_ME",
  "snmpCommunity": "public"
}
